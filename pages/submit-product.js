import React from "react";
import { Layout, SubmitForm } from "../components";
const Submit = () => {
	return (
		<Layout title="Submit Product">
			<SubmitForm />
		</Layout>
	);
};

export default Submit;
